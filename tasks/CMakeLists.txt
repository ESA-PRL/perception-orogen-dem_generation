# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(dem_generationTaskLib)
ADD_LIBRARY(${DEM_GENERATION_TASKLIB_NAME} SHARED 
    ${DEM_GENERATION_TASKLIB_SOURCES})
add_dependencies(${DEM_GENERATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${DEM_GENERATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PCL_LIBRARIES}
    ${DEM_GENERATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DEM_GENERATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DEM_GENERATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${DEM_GENERATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${DEM_GENERATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DEM_GENERATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DEM_GENERATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/dem_generation)

